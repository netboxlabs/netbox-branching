from setuptools import find_packages, setup

setup(
    name='netbox-vcs',
    version='0.1',
    description='A version control system for NetBox',
    install_requires=[],
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
)
